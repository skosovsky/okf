description: "Missing anchor is a link diagnostic"
run:
  path: "."
  strict: false
  check_links: true
  check_orphans: false
expected:
  exit_code: 0
  diagnostics:
    - severity: "WARN"
      message_contains: "anchor not found in target file: ./b.md#ghost"
