description: "Ignored symlinks are absent from the captured bundle and cannot satisfy link targets"
run:
  path: "."
  strict: false
  check_links: true
  check_orphans: false
expected:
  exit_code: 0
  diagnostics:
    - severity: INFO
      message_contains: "broken link"
