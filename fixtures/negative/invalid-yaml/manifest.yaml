description: "Invalid YAML inside a frontmatter block is a conformance error"
run:
  path: "."
  strict: false
  check_links: false
  check_orphans: false
expected:
  exit_code: 1
  diagnostics:
    - severity: "ERROR"
      message_contains: "unparseable concept document: invalid frontmatter: yaml: line 1: did not find expected ',' or ']'"
