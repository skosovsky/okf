description: "log.md date groups must not contain nested headings"
run:
  path: "."
  strict: false
  check_links: false
  check_orphans: false
expected:
  exit_code: 1
  diagnostics:
    - severity: "ERROR"
      message_contains: "log date group contains non-list entry: \"### Details\""
