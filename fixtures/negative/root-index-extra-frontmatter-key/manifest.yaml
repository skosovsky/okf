description: "Root index.md frontmatter may declare only okf_version"
run:
  path: "."
  strict: false
  check_links: false
  check_orphans: false
expected:
  exit_code: 1
  diagnostics:
    - severity: "ERROR"
      message_contains: "root index.md frontmatter should declare only 'okf_version'"
