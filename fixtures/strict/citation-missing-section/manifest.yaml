description: "Citation markers require a Citations section in strict mode"
run:
  path: "."
  strict: true
  check_links: false
  check_orphans: false
expected:
  exit_code: 0
  diagnostics:
    - severity: "WARN"
      message_contains: "citation markers require a bottom '# Citations' section"
