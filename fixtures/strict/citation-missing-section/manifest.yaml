description: "Numeric citation markers alone do not activate legacy Citations policy"
run:
  path: "."
  strict: true
  check_links: false
  check_orphans: false
expected:
  exit_code: 0
  diagnostics: []
