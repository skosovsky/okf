description: "Strict mode validates resource URI when present"
run:
  path: "."
  strict: true
  check_links: false
  check_orphans: false
expected:
  exit_code: 0
  diagnostics:
    - severity: "WARN"
      message_contains: "'resource' is not a valid URI: \"not a uri\""
