description: "Strict mode warns on non-contiguous citation numbering"
run:
  path: "."
  strict: true
  check_links: false
  check_orphans: false
expected:
  exit_code: 0
  diagnostics:
    - severity: "WARN"
      message_contains: "citation numbering should be contiguous starting at 1: got 2, want 1"
