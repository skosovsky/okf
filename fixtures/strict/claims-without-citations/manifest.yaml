description: "Semantic claim detection is delegated to LLM skills, not the Go validator"
run:
  path: "."
  strict: true
  check_links: false
  check_orphans: false
expected:
  exit_code: 0
  diagnostics: []
