description: "BigQuery Table concepts need a top-level # Schema section"
run:
  path: "."
  strict: true
  check_links: false
  check_orphans: false
expected:
  exit_code: 0
  diagnostics:
    - severity: "WARN"
      message_contains: "BigQuery Table concepts should include a '# Schema' section"
