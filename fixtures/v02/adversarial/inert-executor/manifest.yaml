description: "Synthetic policy-bypass executor text remains inert bundle data"
run:
  path: "."
  strict: false
  check_links: true
  check_orphans: true
expected:
  exit_code: 0
  diagnostics:
    - severity: "INFO"
      message_contains: "missing index.md, skipping orphan check for this directory"
      code: "orphan_index_missing"
      spec_ref: "toolkit#orphan-missing-index"
      field_path: "index.md"
